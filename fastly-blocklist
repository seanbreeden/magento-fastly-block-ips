#!/usr/bin/env bash
# Thin wrapper so the tool can live on PATH.
#   ln -s "$(pwd)/fastly-blocklist" ~/.local/bin/fastly-blocklist
exec python3 "$(dirname "$(readlink -f "$0")")/fastly_blocklist.py" "$@"
